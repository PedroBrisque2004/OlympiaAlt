<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.olympia">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Olympia">
        <activity android:name=".registro" />
        <activity android:name=".pressao" />
        <activity android:name=".localtest" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_mapa" />
        <activity android:name=".Tampa" />
        <activity android:name=".Selecao" />
        <activity android:name=".SaoPaulo" />
        <activity android:name=".TelaErro" />
        <activity android:name=".America" />
        <activity android:name=".Volei" />
        <activity android:name=".Beisebol3" />
        <activity android:name=".Beisebol2" />
        <activity android:name=".Beisebol" />
        <activity android:name=".Basquete3" />
        <activity android:name=".basquete2" />
        <activity android:name=".Basquete" />
        <activity android:name=".Football3" />
        <activity android:name=".Football2" />
        <activity android:name=".Football" />
        <activity android:name=".FutebolFem2" />
        <activity android:name=".FutebolFem" />
        <activity android:name=".Futebol2" />
        <activity android:name=".Futebol" />
        <activity android:name=".HomePage" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>